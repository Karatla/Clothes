# 复制为 .env.local 后使用。install 脚本会自动生成。

# 后端地址。
# 用手机通过局域网 IP 访问时，前端会自动把 localhost 换成当前页面的主机名，
# 所以这里保持 localhost 即可，不需要为每台设备单独改配置。
NEXT_PUBLIC_API_URL=http://localhost:3001

# 后端 https 端口，需与 apps/api/.env 里的 HTTPS_PORT 一致
NEXT_PUBLIC_API_HTTPS_PORT=3444
